### Example environment variables (do NOT commit secrets)
# Copy this to .env.local and fill values locally.
NODE_TLS_REJECT_UNAUTHORIZED=0
# Postgres connection (example values)
PGHOST=localhost
PGPORT=5432
PGUSER=postgres
PGPASSWORD=your_local_password
PGDATABASE=postgres
# Recommended for local builds; in production prefer proper SSL
PGSSLMODE=prefer
NEXT_PUBLIC_BASE_URL=http://localhost:9002
DEV_ALLOW_DEFAULT_PG_PASSWORD=true
DB_SSL_REJECT_UNAUTHORIZED=0

# Notes:
# - Keep sensitive values in .env.local which is gitignored.
# - Use PGSSLMODE=prefer to allow optional SSL without refusing builds.