# Example environment variables for local development and deployment
# Do NOT commit real secrets. Copy this to .env.local locally and fill values.

# Postgres connection (project uses PGHOST/PGPORT/PGUSER/PGPASSWORD/PGDATABASE)
PGHOST=localhost
PGPORT=5432
PGUSER=postgres
PGPASSWORD=your_password_here
PGDATABASE=postgres

# Alternatively you can set a single DATABASE_URL with the full connection string
# DATABASE_URL=postgres://user:pass@host:5432/dbname

# Optional runtime toggles
DB_LOG_QUERIES=false
DEV_ALLOW_DEFAULT_PG_PASSWORD=false

# Next/Vercel runtime (example)
#NEXT_PUBLIC_SOME_KEY=
