// Legacy route file moved to `public/signup-legacy.html` to avoid
// a route conflict with the React page at `src/app/signup/page.tsx`.
// Keeping this file intentionally blank so the app page can serve
// `/signup` without collisions.

export {};

